import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { DashboardComponent } from './dashboard/dashboard.component';
import { FoodLogComponent } from './food-log/food-log.component';
import { ProfileComponent } from './profile/profile.component';

//// Define the routes for the applicationconstroutes: Routes = [
//{ path: '', redirectTo: '/dashboard', pathMatch: 'full' }, // Redirect to Dashboard by default
//{ path: 'dashboard', component: DashboardComponent },       // Route to DashboardComponent
//{ path: 'food-log', component: FoodLogComponent },          // Route to FoodLogComponent
//{ path: 'profile', component: ProfileComponent }            // Route to ProfileComponent
//];

//@NgModule({
//  imports: [RouterModule.forRoot(routes)],  // Configure the router at the application's root levelexports: [RouterModule]                   // Export RouterModule to make it available throughout the app
//})
//exportclassAppRoutingModule { }
